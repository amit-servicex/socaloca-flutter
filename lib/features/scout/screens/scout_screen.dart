import 'package:flutter/material.dart';

// TODO: Implement ScoutScreen
// See flutter_migration_docs/screens/ for full spec
class ScoutScreen extends StatelessWidget {
  const ScoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scout')),
      body: const Center(child: Text('TODO: Implement Scout')),
    );
  }
}

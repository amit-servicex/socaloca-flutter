import 'package:flutter/material.dart';

// TODO: Implement ParentalScreen
// See flutter_migration_docs/screens/ for full spec
class ParentalScreen extends StatelessWidget {
  const ParentalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parental')),
      body: const Center(child: Text('TODO: Implement Parental')),
    );
  }
}

import 'package:flutter/material.dart';

// TODO: Implement EndorsementsScreen
// See flutter_migration_docs/screens/ for full spec
class EndorsementsScreen extends StatelessWidget {
  const EndorsementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Endorsements')),
      body: const Center(child: Text('TODO: Implement Endorsements')),
    );
  }
}

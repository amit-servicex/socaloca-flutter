import 'package:flutter/material.dart';
import 'package:socaloca/core/constants/app_strings.dart';

// TODO: Implement RefMatchScreen
// See flutter_migration_docs/screens/ for full spec
class RefMatchScreen extends StatelessWidget {
  RefMatchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('RefMatch'.tr)),
      body: Center(child: Text('TODO: Implement RefMatch'.tr)),
    );
  }
}

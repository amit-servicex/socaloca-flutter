import 'package:flutter/material.dart';

// TODO: Implement RefMatchScreen
// See flutter_migration_docs/screens/ for full spec
class RefMatchScreen extends StatelessWidget {
  const RefMatchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RefMatch')),
      body: const Center(child: Text('TODO: Implement RefMatch')),
    );
  }
}
